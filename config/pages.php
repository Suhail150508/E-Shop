<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Auth page slugs
    |--------------------------------------------------------------------------
    | Login, Register, Forgot Password and Reset Password are edited from
    | Admin → Website Setup (stored in settings). These slugs are hidden
    | from Manage Pages menu and cannot be edited via page edit URL.
    */
    'auth_slugs' => [
        'auth-login',
        'auth-register',
        'auth-forgot-password',
        'auth-reset-password',
    ],

];
